// Audio functionality disabled as requested

// Empty function that does nothing when called
export function playTetrachord(notes) {
  // Audio disabled
  console.log('Audio disabled: Not playing notes', notes);
}
